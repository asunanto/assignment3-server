let should = require('should'),
  sinon = require('sinon')

describe('Test GET', () => {
  it('should return 200 with json object with activity details', (done) => {
    
    let req = {
    }
    let res = {
      status: sinon.spy(),
      headers: sinon.spy()
    }
    let userControl = require('../routes/activities')
    
  })

})